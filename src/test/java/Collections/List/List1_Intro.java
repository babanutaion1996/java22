package Collections.List;

import java.util.ArrayList;
import java.util.List;

public class List1_Intro {
    public static void main(String[] args) {
    // Creating a empty list
        List<String> names = new ArrayList<>();
    // Above, we have created a list that could only store String values, and it is empty for now.
    // What is a size of list above?
    // 0
    // Note: List cannot store primitive data types, However, there is a way around this.

    // Each primitive data type has their own objects.
    // int -> Integer Object
    // double -> Double object
    // char -> Character object
    // boolean -> Boolean object
    // short -> Short object
    // byte -> Byte object

    // Creating a list that could tore int values.
    List<Integer> list = new ArrayList<>();
    // We have creates a list that could store int values.

    //Creating a list that could store double values
    List<Double> listOfDoubles = new ArrayList<>();


    }
}
